# crud-project
This is a simple console-based User Management System built with Python and MySQL. It lets users add, update, delete, search, and view records stored in a database. Using the PyMySQL library, it connects to a table called userinfo to manage basic details like name, age, and phone number.
